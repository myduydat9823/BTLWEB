@model UserListViewModel

@{
    ViewData["Title"] = "Quản lý người dùng";
}

<section class="account-shell">
    <div class="account-card">
        <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
            <h1 class="mb-0">Quản lý người dùng</h1>
            <a class="btn btn-outline-primary" href="/Admin/Articles">Quản lý bài viết</a>
        </div>

        @if (TempData["SuccessMessage"] is string successMessage)
        {
            <div class="alert alert-success">@successMessage</div>
        }
        @if (TempData["ErrorMessage"] is string errorMessage)
        {
            <div class="alert alert-danger">@errorMessage</div>
        }

        <form method="get"
              action="/Admin/UserManagement"
              class="row g-2 mb-4"
              data-ajax-user-filter
              data-ajax-target="#user-list-content">
            <div class="col-md-4">
                <input class="form-control" name="search" value="@Model.Search" placeholder="Tìm theo tên, email..." />
            </div>
            <div class="col-md-3">
                <select class="form-select" name="role">
                    <option value="">Tất cả role</option>
                    @foreach (var role in Model.Roles)
                    {
                        <option value="@role.RoleName" selected="@(Model.Role == role.RoleName)">@role.DisplayName</option>
                    }
                </select>
            </div>
            <div class="col-md-3">
                <select class="form-select" name="status">
                    <option value="">Tất cả trạng thái</option>
                    <option value="active" selected="@(Model.Status == "active")">Đang hoạt động</option>
                    <option value="locked" selected="@(Model.Status == "locked")">Đang khóa</option>
                    <option value="disabled" selected="@(Model.Status == "disabled")">Vô hiệu hóa</option>
                    <option value="deleted" selected="@(Model.Status == "deleted")">Đã xóa mềm</option>
                </select>
            </div>
            <div class="col-md-2">
                <button class="btn btn-primary w-100" type="submit">Lọc</button>
            </div>
        </form>

        <partial name="~/Views/Admin/UserManagement/_UserListTable.cshtml" model="Model" />
    </div>
</section>

@section Scripts {
    <script src="~/js/admin-user-management.js" asp-append-version="true"></script>
}
